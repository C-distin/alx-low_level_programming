#include "main.h"
#include <stdio.h>

/**
* _strlen - find lrngth of string
* @s: string size to be calculated
* Return: string length
*/

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	return (i);
}

